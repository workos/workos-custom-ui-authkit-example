WORKOS_API_KEY=sk_test_...
WORKOS_CLIENT_ID=client_...
WORKOS_COOKIE_PASSWORD=  # Must be 32+ characters. Generate with: openssl rand -base64 32
CSRF_SECRET=             # Optional. Defaults to WORKOS_COOKIE_PASSWORD. Generate with: openssl rand -base64 32
NODE_ENV=development     # Set to "production" for secure cookies
FRONTEND_URL=http://localhost:5176
PORT=3001
